(async function(){
  const id=new URLSearchParams(location.search).get('id'); const root=document.getElementById('productRoot'); const products=await GLOVAERA.getProducts(); const p=products.find(x=>String(x.id)===String(id));
  if(!p){root.innerHTML='<div class="empty-state">Product not found.<br><a class="text-link" href="shop.html">Back to shop</a></div>';return;}
  const price=Number(p.sale_price??p.price); const old=Number(p.sale_price!=null?p.price:0);
  root.innerHTML=`<div class="product-gallery"><img src="${p.image_url||'logo.png'}" alt="${GLOVAERA.escapeHtml(p.name)}"></div><div class="product-info"><div class="product-cat">${GLOVAERA.escapeHtml(p.category||'')}</div><h1>${GLOVAERA.escapeHtml(p.name)}</h1><div class="rating">★★★★★ <span>(New)</span></div><div class="detail-price"><strong>${GLOVAERA.money(price)}</strong>${old>price?`<del>${GLOVAERA.money(old)}</del>`:''}</div><p>${GLOVAERA.escapeHtml(p.description||'Beautiful everyday jewellery selected for the GLOVAERA edit.')}</p><dl class="spec-list"><div><dt>Material</dt><dd>${GLOVAERA.escapeHtml(p.material||'Fashion jewellery')}</dd></div><div><dt>Colour</dt><dd>${GLOVAERA.escapeHtml(p.color||'Gold')}</dd></div><div><dt>Stock</dt><dd>${Number(p.stock||0)>0?'In stock':'Out of stock'}</dd></div></dl><div class="qty-row"><button id="minus">−</button><input id="qty" value="1" inputmode="numeric"><button id="plus">+</button></div><div class="product-actions"><button class="btn btn-secondary" id="addBtn">Add to cart</button><button class="btn btn-primary" id="buyBtn">Buy now</button></div></div>`;
  const qty=document.getElementById('qty'); document.getElementById('minus').onclick=()=>qty.value=Math.max(1,Number(qty.value)-1); document.getElementById('plus').onclick=()=>qty.value=Math.min(Number(p.stock||99),Number(qty.value)+1);
  document.getElementById('addBtn').onclick=()=>{GLOVAERA.addToCart(p,Number(qty.value)||1);document.getElementById('addBtn').textContent='Added ✓';}; document.getElementById('buyBtn').onclick=()=>{GLOVAERA.addToCart(p,Number(qty.value)||1);location.href='checkout.html';};
})();
